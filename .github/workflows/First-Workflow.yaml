  name: Build and deploy React App

on:
  push:
    branches:
      - feature/*

jobs:
  build:
    runs-on: build-server 

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node.js
        run: |
          node -v
          npm -v
        